from pathlib import Path
# from dotenv import load_dotenv

# load_dotenv()

BASE_DIR = Path(__file__).parent.parent
SCHEMA_DIR = BASE_DIR / "schema" / "configuration"
INSTANCE_DIR = BASE_DIR / "schema" / "instances"